{
  "mcpServers": {
    "devdigest": {
      "command": "npm",
      "args": ["--prefix", "mcp", "run", "--silent", "start"],
      "env": {
        "DEVDIGEST_API_URL": "http://localhost:3001"
      }
    }
  }
}
