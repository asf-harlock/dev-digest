import { ConventionsListView } from "./_components/ConventionsListView";

/* Route: /conventions. Thin route entry — the view, its modals, styles,
   constants and helpers are colocated under _components/ConventionsListView. */
export default function ConventionsPage() {
  return <ConventionsListView />;
}
